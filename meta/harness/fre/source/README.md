# Source package

This directory is the `source` part of the seven-part proof package.

It carries:

- provenance and authority pointers
- harvested InfraNodus hook points and scoped lifecycle rows
- normalization rules
- prompt-contract traceability for heavy and bounded execution

The local completeness ledger lives in `provenance.json`.
