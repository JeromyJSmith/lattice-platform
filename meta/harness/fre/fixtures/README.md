# Real Fixture Manifests

These manifests define the first bounded Phase 3 pressure-test fixtures.

They reference real LATTICE artifacts without modifying them. The FRE kernel
uses these manifests to evaluate whether it can reason about real proof,
handoff, and evidence surfaces instead of only toy examples.
